[package]
name = "sow-core"
version = "0.1.0"
edition = "2024"
description = "Shadows of War — Pure deterministic simulation core"
license = "AGPL-3.0-or-later"

[features]
default = ["std"]
std = []
mem_profiler = []
ai-metrics = []

[dependencies]
serde = { workspace = true }
bitfield = { workspace = true }
wyrand = { workspace = true }
log = { workspace = true }
bytemuck = { workspace = true }
libm = { workspace = true }
brotli = { workspace = true }
sow-data = { workspace = true }

[dev-dependencies]
bincode = { workspace = true }
